⚔️ 카카오톡 '검키우기' 자동화 스크립트 가이드
이 가이드는 target_20.py 스크립트를 사용하여 카카오톡 '검키우기' 게임을 자동화하는 방법을 설명합니다.

1. OS별 키 설정 (소스코드 수정)
사용 중인 운영체제(OS)에 맞춰 target_20.py 파일의 98번째 줄을 수정합니다.

Windows 사용자: 단축키 설정을 command → **ctrl**로 변경합니다.

Mac 사용자: 변경 없이 그대로 사용합니다 (command 유지).

2. 좌표값 확인 (Pre-check)
자동화를 실행할 카카오톡 창의 정확한 위치를 파악하는 단계입니다.

PC 카카오톡을 실행하고, '검키우기'를 진행할 채팅방을 화면에 띄웁니다.

pointer_loc_check.py 파일을 실행합니다.

마우스 포인터를 카카오톡 채팅방 입력창이나 특정 위치에 두고 좌표(x, y)를 확인합니다.

3. 좌표값 적용 (Configuration)
확인한 좌표값을 메인 스크립트에 적용합니다.

target_20.py 파일을 엽니다.

88번, 89번 줄로 이동합니다.

앞서 파악한 x, y 좌표값을 해당 라인 변수에 각각 입력하여 저장합니다.

4. 스크립트 실행 (Run)
설정이 완료되면 자동화를 시작합니다.

카카오톡 창이 화면에 떠 있는 상태인지 확인합니다.

target_20.py를 실행합니다.

💡 동작 로직
스크립트는 실행 후 다음과 같은 로직으로 작동합니다:

Condition Check:

✅ '강화' 이미지가 화면에 있는 경우: 자동으로 해당 버튼을 클릭합니다.

❌ 이미지가 없는 경우: 채팅창에 직접 /강화 명령어를 입력합니다.